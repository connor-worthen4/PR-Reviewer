# ─── Required ────────────────────────────────────────────────────────────────

# Comma-separated list of repos to watch (e.g. yourname/project1,yourname/project2)
GITHUB_REPOS=

# Absolute path to the claude binary (find with: which claude)
CLAUDE_PATH=

# Your GitHub username (used to filter bot's own comments when not using GitHub App)
GITHUB_USERNAME=

# ─── Project Directories ─────────────────────────────────────────────────────
# Maps repos to local paths so !fix and !fix-lint can run in the correct directory.
# Format: owner/repo=/absolute/path,owner/repo2=/absolute/path2
PROJECT_DIRS=

# ─── GitHub App (optional, for bot identity) ─────────────────────────────────
# When configured, reviews and comments post under a dedicated bot account
# with a [bot] badge. Falls back to gh CLI if not set.

GITHUB_APP_ID=
GITHUB_APP_INSTALLATION_ID=
GITHUB_APP_PRIVATE_KEY_PATH=

# ─── Discord Notifications (optional) ────────────────────────────────────────
# Leave empty to disable. If using the Discord Claude Bridge, use the same bot token.

DISCORD_BOT_TOKEN=
DISCORD_CHANNEL_ID=

# ─── Tuning ──────────────────────────────────────────────────────────────────

# Poll interval in milliseconds (default: 180000 = 3 minutes)
POLL_INTERVAL_MS=180000

# Set to 1 for verbose debug logging
DEBUG=0
